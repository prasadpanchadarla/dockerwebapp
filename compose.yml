services:
  devopsdb:
    container_name: devopsdb
    image: imgdb:v1
    ports:
      - "3306:3306"

  application:
    container_name: appcont
    image: imgapp:v1
    ports:
      - "1234:8080"
    depends_on:
      - "devopsdb"
