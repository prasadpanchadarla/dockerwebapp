services:
  devopsdb:
    image: imgdb:v1
    deploy:
      replicas: 3
    ports:
      - 3306

  application:
    image: imgapp:v1
    deploy:
      replicas: 3
    ports:
      - 8080
    depends_on:
      - "devopsdb"
