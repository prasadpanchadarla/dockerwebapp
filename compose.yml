services:
  devopsdb:
    container_name: devopsdb
    image: dbimg
    ports:
      - "3306:3306"

  application:
    container_name: appcont
    image: appimg:v1
    ports:
      - "1234:8080"
    depends_on:
      - "devopsdb"
