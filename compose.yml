services:
  devopsdb:
    image: dbimg:latest
    deploy:
      replicas: 3
    ports:
      - 3306

  application:
    image: appimg:latest
    deploy:
      replicas: 3
    ports:
      - 8080
    depends_on:
      - "devopsdb"
